# =============================================================================
# Investor Copilot API - Core Dependencies
# =============================================================================
# This is the minimal set the service currently needs.

# --- Web framework + streaming ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sse-starlette>=1.8.2       # SSE response support
httpx>=0.27.0              # async HTTP client + TestClient

# --- Validation ---
pydantic>=2.7.0

# --- LLM ---
openai>=1.30.0             # OpenAI Python SDK - structured outputs, streaming
google-genai>=0.1.1        # Google GenAI Python SDK - Gemini

# --- Utilities ---
python-dotenv>=1.0.0
yfinance>=0.2.54

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# =============================================================================
# Suggested additions (add only what you actually use):
#   - pandas / numpy        - if your portfolio math gets non-trivial
#   - asyncpg / aiosqlite   - only if you persist sessions in a real DB
#   - tenacity              - if you implement retry policies
#   - cachetools            - if you implement request-level caching
# =============================================================================
